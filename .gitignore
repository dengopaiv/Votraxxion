# Build outputs
build/
dist/
*.o
*.obj
*.pyd
*.dll
*.so
*.lib
*.exp
vsim

# Python
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/

# Audio scratch
*.wav

# Local tooling
.claude/

# NVDA add-on build outputs (built by nvda-addon/package.py)
*.nvda-addon
nvda-addon/addon/synthDrivers/*.dll

# Golden fingerprints are captured on demand, not committed -- see
# tools/goldens.py.  The one exception is the checked-in reference under
# tests/data/, which is committed deliberately.
/goldens/
